# Weport v0.8.2

## ⚡ 性能优化：后台常驻内存削减 ~500MB（8 进程 → 3~6 进程）

**症状**：开机自启（托盘常驻）模式下，任务管理器里出现多个 Weport/WeFlow 进程，常驻内存合计约 800MB，对低配机器造成明显负担。

**优化内容**（后台 `--background` / 托盘模式下生效，主窗口行为不变）：

1. **静默启动不再预建主窗口** — 之前 `--background` 也会先创建隐藏主窗口再隐藏（渲染/GPU/网络子进程约 1GB）。现在静默启动零窗口，托盘点击 / 通知点击时按需创建，省掉整套隐藏渲染进程。
2. **WCDB 宿主进程不再拉起 GPU 子进程** — 宿主只做数据库工作、不渲染任何窗口，但 Electron 43 仍会默认拉起软件 GPU 子进程（~60-100MB）。已通过 `disable-gpu` + `in-process-gpu` 实测消除。
3. **WCDB 宿主与数据库连接延迟到真正需要时** — 静默启动且未开启消息提醒时，不再开机即拉起宿主进程并打开整个微信数据库（~900MB）；首次打开窗口 / 导出 / 开启提醒时才按需连接。联系人显示名缓存已持久化到磁盘，不受影响。
4. **微信 CDN 头像请求头拦截延迟注册** — 静默启动不再提前初始化网络栈，首次窗口 / 弹窗时注册（幂等）。
5. **更新检查延迟到窗口首次打开** — 静默启动不再做无用更新检查（横幅无窗口可送达）。

**默认行为调整**：首次安装后，开机自启、启动时隐藏到托盘、关闭窗口最小化到托盘默认全部开启（Run 键自动写入 `--background`，登录后托盘常驻、不弹窗）。

**验证**：静默启动 + 消息提醒关闭 → 3 个进程（主 + GPU + 网络服务），~225MB；开启消息提醒 → 6 个进程，宿主 GPU 子进程消失；截图 QA 管线全部通过。

[MIT License](https://github.com/Panther114/Weport/blob/main/LICENSE)
