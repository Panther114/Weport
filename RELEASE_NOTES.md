# Weport v0.9.5

## 分析页新增可视化（词云 / 画像雷达 / 活跃日历 / 群热力图）

- 全局分析：新增交流画像雷达（文字/图片/语音/视频/表情/深夜活跃六维）、活跃日历（近 12 个月每日消息量热力）、高频词云（CJK 二元分词 + 停用词过滤，最多扫描 15 万条文本消息，10 分钟缓存）
- 群聊分析：新增「画像」页签（成员画像雷达 + 24×7 活跃热力图）；成员详情新增高频词云（Top 40）
- 全部新图表跟随浅蓝/黑白主题；echarts-wordcloud 在 ECharts 6 下验证可用

## 本地 MCP 服务（只读，AI 宿主接入）

- 基于 @modelcontextprotocol/sdk 的 Streamable HTTP 服务：`http://127.0.0.1:5032/mcp`，Bearer token 认证（`mcpToken` 自动生成并 safeStorage 加密，可回退 `httpApiToken`）
- 13 个只读工具：会话列表 / 会话消息 / 消息搜索 / 联系人 / 群聊列表 / 群成员 / 群统计（时段+媒体+热力图）/ 朋友圈时间线 / 朋友圈统计 / 全局统计 / 联系排行榜 / 每日活跃 / 高频词 —— 全部落在既有只读服务上，无写入/发送能力
- Claude Desktop 等仅支持 stdio 的宿主：通过 `resources/mcp/mcp-stdio-bridge.cjs` 桥接（esbuild 单文件自包含，任意 Node ≥ 18 可运行，无需 NODE_PATH）
- 配置键：`mcpEnabled`（默认开）、`mcpPort`（5032）、`mcpHost`（127.0.0.1）、`mcpToken`（自动生成）

# Weport v0.9.4

- 修复自动更新下载完成后不安装、不重启的问题；托盘静默启动首次打开窗口时会补查更新。
- 优化朋友圈启动与加载：缓存优先、缩略图优先，完整图片和视频只在预览或下载时获取。
- 修复 Windows 版朋友圈 WASM 解密资源缺失、解密失败重试和媒体重复下载问题。
- 优化朋友圈新动态检测、作者头像、评论表情和群成员头像缓存，减少重复数据库与网络请求。
- 优化启动阶段的数据库连接、会话读取和后台预热，避免多个任务争抢 WCDB 宿主。
- 群聊分析复用聚合结果，修复缓存命中误报失败，并补齐成员头像本地化。
- 朋友圈新增持久化页面/作者缓存、日期跳转、评论搜索、虚拟滚动和手动刷新。
- WeportAI 兼容更多 OpenAI 接口：规范化请求地址，隔离 DeepSeek 专用参数，修复不完整工具历史导致的 400，并保留更完整的错误信息。
- 新增双人年度报告、数据备份、本地 HTTP API 和 Windows Hello 认证能力。

# Weport v0.9.3

## 内存优化（实测常驻 ≈524 MB，较 v0.9.2 的 ≈844 MB 下降 38%）

### WCDB 宿主进程改为纯 Node 模式

- 宿主仍由硬链接 `WeFlow.exe` 启动（-1006 文件名检查不变），但以 `ELECTRON_RUN_AS_NODE=1` 运行 —— 同一二进制按纯 Node.js 执行，不再初始化 Chromium 浏览器进程
- 实测：宿主工作集 104.1 MB + 网络服务子进程 50.6 MB → **44.9 MB 单进程**（省 ≈110 MB）
- koffi（N-API）无需重编译；打包版宿主脚本与 koffi 复制到 `resources/host/`（纯 Node 读不了 app.asar），经 `NODE_PATH` 解析

### 隐藏窗口内存回收（托盘模式）

- 主窗口隐藏到托盘超过 5 分钟 → 卸载渲染层（`about:blank`），省掉渲染进程堆与 GPU 缓冲；托盘点击 / 二次启动即恢复（重载后约 0.5s 显示）
- 导出进行中不回收（进度事件目标需存活）；QA/截图模式不回收；状态全部在主进程/配置侧，恢复无功能丢失

### `--background` 关闭硬件加速

- 开机自启托盘常驻时调用 `app.disableHardwareAcceleration()`：GPU 进程消失（实测省 ≈130 MB 工作集 / ≈312 MB 私有提交），窗口走软件光栅；Windows 通知弹窗的原生玻璃面板不受影响（D3D11 在原生侧）

### Chromium 基础调优

- `js-flags --max-old-space-size=384 --max-semi-space-size=4`：V8 堆封顶，提前触发 GC 降低稳态内存
- `disk-cache-size 16MB`：HTTP 缓存上限（头像已走本地磁盘缓存）
- 两个窗口 `spellcheck: false`：关闭拼写服务与词典内存

### 通知弹窗独立瘦身入口

- 弹窗改加载 `dist/popup.html`（独立 vite 入口，仅 NotificationWindow 依赖），不再解析含 ECharts/html2canvas 的整包 —— 弹窗渲染进程堆显著下降，首条通知出现更快
- 弹窗字体与主应用保持一致（`popupBase.css`，修复此前字体回退到系统默认的问题）

## 修复

- 截图 QA 的 AI 面板诊断探针曾内嵌 TypeScript 类型注解（`as`/`: Element`），探针本身报 SyntaxError 掩盖真实原因 —— 已改为纯 JS

## 验证

- 类型检查 + 完整截图冒烟（12 屏全通过，弹窗 stddev ≥ 60）
- 宿主纯 Node 冒烟：`wcdb_init [SecurityStatus:0]`（-1006 文件名检查通过）
- 实测内存（托盘 + 开窗 + 推送常开）：主进程 137 + 渲染 105 + 弹窗 77 + 宿主 77 + 工具进程 128 ≈ **524 MB**（v0.9.2 同场景 ≈844 MB）

# Weport v0.9.2

## 稳定性与安全加固（25 项问题审查 + 修复）

### 安全
- **AI 聊天 Markdown XSS 修复**：链接/图片 URL 协议白名单（拦截 `javascript:`/`data:`/`file:`）、失败回退纯文本转义、渲染层 CSP（生产严格策略）、主/通知窗口导航守卫（外链一律系统浏览器打开）
- **WeClone CSV 公式注入防护**：以 `= + - @` 制表符/回车开头的内容加撇号前缀，恶意会话内容不再能被 Excel 当公式执行
- **AI API 地址安全校验**：仅允许 https（localhost 例外），密钥不再可能明文传输；隐私文案修正（密钥会随请求发给所配置的 API）

### 导出可靠性
- **中途失败不再产出"完整"残档**：批次读取失败显式上报错误（此前静默截断，部分行被当作完整导出写入）；8 个格式器统一检查
- **原子写入**：TXT/Markdown/SQL/WeClone/HTML 先写同目录临时文件再重命名覆盖；写中途失败保留旧文件、流错误立即捕获（此前崩溃/挂死或截断旧档）
- **同名会话不再互相覆盖**：同一批导出内同名会话（如多个「一家人」）自动加序号去重
- **账号切换错位修复**：选择账号时把该账号密钥写回全局配置（此前导出/连接可能用到上一个账号的密钥）；仅在实际不一致时写回，避免启动连接抖动
- **CSV/导出记录按账号隔离**（wxid 前缀）；Windows 保留设备名（CON/PRN/NUL 等）自动改名；更新版本比较支持预发布后缀
- **导出可取消**：进度事件携带 taskId，导出页新增「取消导出」按钮；取消后自动清理本次已写出的部分文件；未知任务 ID 不再假成功

### WCDB 宿主进程
- **请求串行化**：原生 core 不可重入，所有宿主消息排队执行（此前 testConnection/close 可能与其他在途请求交错，打到空句柄）
- **超时自动重建**：180s 卡死不再需要重启应用；宿主退出（含退出码 0）立即失败所有在途请求
- 宿主硬链接新鲜度检查增加修改时间维度；关闭消息提醒后释放 WCDB 连接与监控管道

### WeportAI
- 切换会话自动中止旧任务（此前 running 卡死、停止按钮打错目标）；删除运行中的会话会先中止
- 流式响应：无数据 90s 看门狗（服务端挂起不再无限等待）、流末尾无换行的尾块不再丢弃
- 会话索引/消息文件原子写入（崩溃不再剩半个 JSON）；快速切换时丢弃过期响应

### 其他
- 通知弹窗：底部定位随高度变化重新贴底；玻璃背景不再把弹窗自己拍进去（截图时临时启用内容保护）
- `--background` 静默启动后手动启动应用会唤出主窗口（此前无反应）
- 事件订阅退订只移除本回调（此前 removeAllListeners 会误杀同频道其他订阅者）

## 朋友圈媒体解密修复与提速

- **灯箱「图片无法查看」修复**：点击图片此前会用加密的 CDN 密文 URL 渲染（必然破图）。现在灯箱直接使用网格已解密的本地全图（weport-media:// 毫秒级显示），前后翻页同样命中本地文件；视频标记正确
- **「加载失败」修复**：32 位字级 XOR 曾用 `writeUInt32BE` 导致符号位溢出抛错（约半数字节块崩溃）——改为 Uint32Array 视图 XOR，同时提速约 4.7 倍（实测 2ms/4MB）
- **解密提速**：keystream 按密钥缓存（LRU 64MB，同密钥媒体免重复生成）；CDN 下载并发限 4（此前几十张并发争抢带宽触发超时）；超时 15s→30s；失败资源 5 分钟快速失败记忆（死链不再反复重试）
- 真实数据验证：3 张真实 CDN 图片解密全部成功，缓存命中 0–2ms

## 分析界面（微信数据分析）

- 入口两个卡片（全局分析 / 群聊分析）**始终并排**：修复 auto-fit 网格在 flex 居中容器内塌缩成单列的问题，全屏与非全屏均保持左右并排
- 群聊分析：顶栏收窄靠右，群列表占满左侧；新增排序切换（消息数 / 人数，默认消息数）；群列表显示消息条数
- 媒体构成：饼图改用可区分的专属色板（文本/图片/语音/视频/表情包/链接文件各一色，黑白主题灰阶）；列表每类配专属图标
- 24 小时活跃度柱形加宽、柱间距大幅缩小
- 成员列表头像提速：去掉每次进群都强制全量重算（缓存命中即时返回）

## 交互动效补全

- 开关/勾选框：打勾瞬间弹跳 + 文字轻弹（此前完全无反馈）
- 通用按压反馈：主要按钮/图标按钮/分段控件按下缩放；页签激活弹跳动画

## 表情显示修复（上版内容保留）

- 补齐全部微信表情资源：此前只打包了 `face` 类别（75 个），动物 / 祝福 / 手势 / 其他等类别缺失导致部分表情（[动物] 等）显示为空白/破图。现在与 WeFlow 一致，完整打包 wechat-emojis 全部 109 个表情（5 个类别），朋友圈正文、评论与通知弹窗中的表情均可正常显示。

## 朋友圈布局与体验

- 顶部横条（总动态 / 好友 / 我的动态 + 防删除 / 导出）合并进左侧筛选栏顶部，整体高度大幅缩减，信息更紧凑
- 评论区的自定义表情包显示修复（本地协议链路统一）

## 朋友圈媒体加载提速（对齐 WeFlow 并优化）

- 网格缩略图：大图在**主进程**用 nativeImage 一次性缩到 720px（JPEG），IPC 载荷与渲染内存降低 20~50 倍，网格不再长时间停留在「解密中」；全图始终写入磁盘缓存
- 灯箱预览改为直接读取磁盘缓存全图（weport-media:// 本地协议，零网络零 IPC）；下载也走全图文件而非缩略图
- 视口懒加载、内存缓存与磁盘缓存命中路径保持不变（不增加 CPU / RAM 常驻开销）

## 分析（微信数据分析）

- 「确定性数据分析」更名为「微信数据分析」，入口标题上方新增浮动动画图标
- 消息类型构成：六类消息全部配图标 + 专属主题色；统计卡片 / 排行榜 / 成员数字全部着色并带悬停动效
- 卡片 hover 过渡补全：标题、描述、箭头随图标一起平滑动画

## 色彩主题（双主题）

- 新增「设置」页签（顶栏最右侧），独立于「消息通知」
- 设置页含：开机自启 / 启动时隐藏到托盘 / 关闭最小化到托盘 + 色彩主题 + 关于更新
- 色彩主题两种模式：**缤纷**（默认，现代六色 accent 体系，克制不刺眼）与**黑白**（经典灰阶），应用于文字、图标、图表、数字的整个 UI；选择即时生效并持久化保存

## 其他修复

- 开机自启：清理参考项目 WeFlow 遗留的登录项（此前会随系统开机拉起 WeFlow）；修复 Run 键比较逻辑（此前每次启动都会重写）；开关以系统实际状态为准，失败不再误显示「已开启」
- 数字滚动动画（CountUp）跳动期间恒为整数
- 新增 `npm test`：类型检查 + 截图冒烟测试一键执行

# Weport v0.9.1

## 头像加载深度修复（消息弹窗 / 朋友圈 / 群聊 / 排行榜）

此前头像总是「转圈 / 占位一段时间」才显示，根因有三层，本次全部修复：

1. **本地头像优先** — 微信 `head_image.db` 里本来就有每个人的头像数据（实测 87%–100% 覆盖率），此前只在「没有 CDN URL」时才读取。现在优先读取本地头像，离线、不过期、即时显示。
2. **头像磁盘缓存 + 本地协议** — 新增 `avatarCacheService`：所有头像（head_image 或 CDN）统一落盘到 `cache/avatars/`，通过 `weport-media://` 本地协议即时渲染。CDN URL 过期 / 403 不再影响显示；渲染进程零网络请求。启动预热（前 600 会话）自动完成全部头像落盘，之后弹窗、朋友圈、群成员、排行榜均为毫秒级加载。
3. **关键修复：本地协议 URL 格式** — 旧格式把盘符放进 host（`weport-media://C:/…`），Chromium 会把 `C:` 规范化为 host `c`（冒号被当作端口分隔符），路径解析丢失盘符冒号 → 永远 404。新格式 `weport-media://local/<百分号编码的完整路径>` 彻底解决，同时修复了朋友圈视频预览的同类隐患。
4. **渲染层提速** — 头像加载队列并发 3 → 8、批间延迟 10ms → 2ms；本地协议 URL 不再排队直接加载。
5. **缓存命中校验** — 缓存里的本地 URL 若文件已不存在（如缓存目录变更）会自动重新解析，不会显示破图。

实测（真实数据库）：60 个会话的探针样本 100% 返回本地协议 URL、100% 文件可解析、0 个 CDN 回退；283 个头像文件落盘。

# Weport v0.9.0

## 新功能

### 朋友圈（v0.9 新增模块）

- 全量浏览本地微信 sns.db 的历史朋友圈：分页时间线、按发布者 / 关键词 / 日期快速筛选，点击发布者可查看其全部动态
- 媒体解密预览：图片 / 实况照片 / 视频经本地解密后直接在应用内预览（视口懒加载 + 内存/磁盘缓存，滚动长列表时 CPU 与内存占用显著降低）
- 导出：JSON / HTML / ARKME JSON / Markdown 四种格式，支持时间范围预设（全部 / 30 天 / 一年 / 自定义）与媒体文件选择，后台进度、可取消
- 防删除：一键安装朋友圈删除拦截触发器；也可单独删除某条记录
- 旧版朋友圈媒体缓存自动检测与迁移

### 分析（v0.9 新增模块）

- 统一入口：全局分析 / 群聊分析 两个大卡片选择
- 全局分析：消息类型构成、24 小时 / 星期 / 月度分布、我的每日消息、联系排行榜 Top 20、统计排除名单（公众号等一键排除并即时重算）
- 年度报告：年度核心好友、每月聊得最多的人、7×24 活跃热力图、深夜王者、互相奔赴、社交主动性、响应速度、高频短语、朋友圈年度统计；可导出报告图片（按分区 PNG）
- 群聊分析：群成员列表（群主 / 好友标识、消息占比条）、消息排行 Top 20、24 小时活跃时段、媒体构成；点击成员查看画像（统计卡片、活跃时段、高频短语、常用表情、分页消息记录）并可导出 CSV

## 性能与体验

- 朋友圈媒体视口懒加载：进入可视区域（提前 600px）才解密，避免长列表一次性拉取全部媒体
- 列表与弹层组件 React.memo 化 + 稳定回调，减少无效重渲染
- 统计数字滚动动画（CountUp，尊重系统"减少动效"设置，数据后到不重播）
- 页签切换动画平滑化；动态列表入场错峰动画；媒体骨架微光占位
- 所有弹层支持 Esc 关闭；空状态统一为图标化提示
- 顶栏页签可横向滚动（窗口较窄时不挤压）

## 技术

- 服务层（朋友圈 / 全局分析 / 群聊分析 / 年度报告）为 WeFlow 近原样移植，WCDB 引擎层零改动（原生 FFI 此前已就绪）
- 新增 weport-media:// 本地媒体协议（特权注册，webSecurity 保持开启）
- 年度报告生成在独立 worker 线程中运行，进度跨窗口广播
- QA：新增 WEPORT_V09_DUMP=1 端到端转储模式（脱敏演示数据驱动全部 v0.9 页面，断言 DOM 与 console 错误，失败非零退出）

---
