# Weport v1.1.0

- 修复图片密钥取不到、导致所有聊天图片都读不出：密钥码现在直接从微信 MMKV 缓存的文件名 `key_<code>_*.statistic` 读出来并离线派生，不再依赖 `wx_key.dll` 返回（微信 4.1.x 上常返回空）与「先扫内存」
- 图片密钥派生后会用本账号自己的 `*_t.dat` 模板校验归属，成功即按账号保存；同一账号只需获取一次，重启与微信升级后依然有效
- 修复看图后下一轮请求被网关拒收（400/422 `Input should be a valid string`）：工具结果的 `content` 保持字符串，图片改为随后的独立 user 消息下发
- 新增「图片输入」开关（默认开）：关掉后看图工具只回报「谁在什么时候发了一张图」，供不支持视觉的模型或网关使用
- 修复导出的图片停留在缩略图：微信对同一张图存三档（原图 / 显示版 / 缩略图），导出以前只有「原图 / 非原图」两态，现在严格按档位升级，不再把上一轮遗留的缩略图产物当成解密结果
- 修复导出的语音一条文件都没有：打包版把 `silk.wasm` 的路径拼成了 `resources/resources/node_modules/...`，语音解码全部失败；现在按模块自身位置解析，路径推断不出来时也继续尝试解码
- 语音导出失败不再静默：导出完成提示会给出未能导出的语音条数与原因（多数是微信里还没有完整语音文件，先在微信里播放一次即可）
- wxgf(HEVC) 图片依赖的 ffmpeg 改为复用系统探测（PATH / WinGet / scoop / brew / `WEPORT_FFMPEG`），与背景视频共用一份实现
- WeportAI 一轮对话里只发一次工具调用、只能一步一个工具的问题修复：并发安全的工具并行执行（默认 4 路），写记忆/同步类工具仍单独成顺序屏障，结果按提交顺序回填
- 每个工具轮次结束立即落盘并出图：工具卡不再整轮堆在同一个气泡顶部，思考与调用按时间顺序交错显示
- 上下文几乎总是全量送达：每一步都可以在日志里看到 prompt / 命中 / 未命中 token 与缓存命中率，不再只能凭感觉
- 实时速度读数改为只统计「首 token 之后」的解码时间，并按中英混排重新估算 token，流式输出时不再一路向下掉
- 修复生成过程中无法上滚阅读：滚轮、触摸或拖动滚动条即停止自动跟随，只有回到最底部（或点「回到最新」）才恢复
- 通知玻璃的实时桌面折射帧率从约 0.7fps 提到约 14fps：改为直接读取玻璃所在的那一小块屏幕（koffi 调用 GDI，单帧 5~22ms，原路径为 150~208ms）
- 新增 `WEPORT_GLASS_NOCAPTURE=1`：需要排查采集相关问题时一键回到旧的抓帧路径

# Weport v1.0.1

- 修复通知弹窗「先弹出来、再抖一下」：窗口现在等渲染层量好卡片尺寸再显示，第一帧就是最终尺寸与位置
- 新增「滑入滑出」弹窗动效（默认）：从离弹窗最近的那条屏幕边滑进来，退场沿原路滑出；按位置自动选边（左上角往右滑入，顶部居中从上往下），旧版「淡入缩放」仍可在设置里选
- 入场动画改为等窗口真正显示后再起跑：不再出现「卡片从屏幕边上闪一下」，整段位移都看得见
- 滑入滑出放慢到 1.05s / 0.62s，并且卡片起点真的在屏幕外：以前它只能从屏幕内 20px 那条线钻出来，看起来是凭空冒出
- 修复弹窗滑动时的抖动：入场结束收回窗口时卡片不再跳一下、退场前窗口先放开再滑，不再出现「滑到一半被切断」
- 通知玻璃补了三层真实感：沿圆角走的镜面高光、可见的玻璃厚度（上缘亮 / 下缘暗）、贴着下沿的接触阴影 + 环境阴影两段式投影
- 修复通知投影被切掉：兜底投影原本 6px/18px 而窗口只留 8px 边距，影子被窗口边界裁掉，看起来「不跟圆角走」
- 修复通知文字色前几秒是白的、之后才变黑：文字极性本来就由玻璃填充色固定，现在第一帧就是对的
- WeBot 失败的运行不再写进笔记板：错误只留在运行记录里（带完整原因、时刻与耗时），结论板只放结论
- WeBot 笔记取消已读/未读：「只看未读」筛选与已读按钮全部移除，改在每条笔记右上角提供 ✕ 逐条删除
- WeBot 笔记正文支持 Markdown 渲染（标题、列表、加粗不再以源码形式露出来）
- 修复一条失败的运行会永远显示「运行中」：运行结束事件现在成对上报，成功与失败都会收尾
- WeportAI 与 WeBot 新增「同步聊天记录」工具：可强制重读某个会话或全部会话（清掉分页游标、统计与首屏缓存），并返回此刻真正的最新一条消息
- 两个 agent 现在都被告知「你已经读到的聊天内容可能过期」：涉及「最新 / 今天 / 有没有新消息」时先同步再回答
- 修复人格克隆回复被塞进一个气泡：连发的多条短消息现在各占一条气泡（此前表现为「多行长诗」与多出来的空行）
- 通知弹窗尺寸上报不再等 50ms 定时器，首帧直接上报；同一条通知重复投递会被忽略（不再重播入场动画）
- 首次通知（窗口冷启动）实测约 200ms 内出现，之后每条 10~30ms；新增 `.ui-probe/probe-popup-latency.mjs` 逐段测量
- Linux 通知改走系统通知服务（mako / dunst / swaync 等 D-Bus），点击通知可打开或聚焦微信
- Linux 新增「系统通知」方式设置（自动 / 强制系统通知 / 只用应用内弹窗），检测不到服务时自动回退
- 修复 Linux 下每条消息都弹出「共享屏幕」授权框：回退到应用内弹窗时也不再抓取桌面
- 修复「跟随微信消息免打扰」不计入「屏蔽 n 个会话」：免打扰的会话现在算进总数，并注明其中有几个来自微信免打扰
- 会话过滤对话框里，微信标了免打扰的会话会显示「免打扰」标记
- WeportAI 与 WeBot 的 `@` 选择器改为**按最近聊天排序**（私聊与群聊混排）：此前群聊整块排在前面，列出 60 条后一条私聊都看不到
- 已经引用过的会话在 `@` 选择器里灰掉并标「已引用」，键盘上下键会跳过它们
- 新增「看聊天里的图片」工具：agent 可以真正查看聊天里的图片（作业照片、截图、通知单），不再只能看到 `[图片]`
- WeBot 任务提示词要求：结论依赖图片时必须实际看图，不得凭猜测描述图片内容
- 图片以独立内容块发给模型（OpenAI / Anthropic / Gemini 各自的格式），模型不支持看图时如实说明
- AI 面板显示「已附上 N 张图片」的标记，可确认这次回答是否基于画面
- 移除模型定价估算：单价与本轮花费不再显示（models.dev 覆盖不到常用服务商，显示的多数是「未定价」，有值时也只是估算）
- WeBot 任务执行时间改为 12 小时制 + 上午/下午，小时是 1–12；卡片与「下次执行」也用同一读法
- WeBot 任务卡片可查看**运行记录**：每次运行的时刻、结果、耗时与完整错误，不再只有一行「上次失败」
- 运行中的任务在界面上立即可见（运行开始时就会出现在运行记录里）
- 网络失败不再只报「fetch failed」：展开为「域名解析失败 / 连接被拒绝 / 连接超时」等原因，并带上主机名
- 连接阶段的网络失败会自动重试两次（DNS 抖动、连接建立失败）；连接被重置不重试，避免重复计费
- 修复 macOS 获取密钥提示里的错误指引：不再声称「重新签名 Weport 就能附加微信」——正式版微信不带 get-task-allow，任何进程都附加不了，提示改为指向手动填密钥与磁盘推导图片密钥
- 修复 macOS 几处提示仍写成旧名字 WeFlow，改为 Weport
- 修复 `@` 引用会话在 WeportAI 与 WeBot 里一直提示「先连接微信」：返回体字段名解析错误，已连上却读不到会话
- 通知渐变预设从 9 套扩到 51 套，全部改用公开渐变库里真实用过的配色，每套两端亮度差 ≥ 0.20，不再是几乎同色的浅灰
- 通知默认填充改为「晴空」渐变，替换原来的蓝紫
- 人格克隆改为逐段提炼：聊天记录按时间切成若干段分别交给 AI，再归并成人格档案，不再从全量里随机抽 250 块
- 修复分段归并不收敛：同一段摘要被反复重写，一次生成能卡在同一层十几分钟
- 修复生成中途失败时面板显示「生成完成」：成功与失败现在用两个不同的终态，失败会直接给出原因
- 修复重新生成克隆后，卡片仍显示上一版的段数 / token / 时间，改设置会静默失败
- 修复对方只发「在吗」这类短消息时没有语气样本可参照，回复滑回助手腔
- 人格克隆不再截断：单会话上限从 15 万条提到 1200 万条，卡片上不再出现「数据量过大已截断」
- 人格克隆新增本地风格指纹：标点习惯、消息长度分布、表情密度、高频词与搭配全部本地统计后写入档案
- 人格克隆的语气样本改为按当前话题检索本人原话，不再把固定例句写进提示词
- 修复英文语料下风格指纹全是 the / ing 这类词缀碎片，改为按整词与二元搭配统计
- 人格档案生成不再被 12000 字截断，篇幅上限提到 12 万字
- 修复人格克隆「知识截止」不生效：提示词不再声明「截止之后一概不知」，改为断言记录里的事都亲历过
- 人格克隆新增分段失败兜底与统计：哪几段没提炼成功会写进档案并显示在卡片上
- 生成克隆时可选择是否移除敏感信息（默认开启）；关掉后不再替换占位符、也不再做二次审查
- 每个克隆可单独设置敏感话题怎么处理：以本人的方式带过去（默认），或完全不回避
- 命令面补齐：`weclone.generate` 可指定是否脱敏，`weclone.clones` 显示每个克隆的策略，新增 `weclone.settings`
- 人格克隆提示词全面重写：去掉所有预设示例台词与「逐字模仿这些句子」，改为按判据约束多面性，避免被浓缩成一个标签
- 修复切换页面后看不到克隆生成 / 导出 / 连接进度：进度改为应用级订阅，并记住 WeClone 页所在的分区
- 新增左下角常驻任务条：任何页面都能看到正在跑的任务、已用时间，并可一键跳转或取消
- 托盘隐藏销毁窗口后重新打开，仍在运行的任务会从主进程恢复进度、日志与已用时间
- 克隆卡片新增生成深度小结：读了多少条、切了多少段、耗时、token 用量、是否脱敏
- 生成耗时与 token 用量显著上升（本机 10 万条消息约三十余段、十余分钟），换来的是档案不再只有空话
- 修复 `@` 选择器里的搜索框点不动、打不进字：它是只读的展示框，点一下就把焦点从输入框抢走（在 WeBot 里还会顺手把整个选择器关掉）
- 所有弹层改到独立浮层渲染：`@` 选择器、快捷动作菜单、取色器、朋友圈日历在 WeBot 顶部、设置页底部、侧栏里都不会再被裁掉或压在别的东西下面
- `@` 选择器/取色器在空间不足时会自动翻到另一侧，贴着屏幕边缘时自动收回视口内
- 引用会话不再同时出现在输入框和下方：只在输入框外的 chip 区显示一次，输入框里不留 `@名称`
- 修复删除克隆提示「删除失败（目录不为空）」但其实已经删掉：删除改成带重试并在抛错后复核结果，Windows 上被杀毒/索引短暂占住文件不再误报失败
- 修复通知「投影」滑块完全没反应：这个变量写了但没有任何样式读它，而且窗口尺寸没给投影留空间，画出来也会被裁掉
- 修复通知「折射强度」「玻璃模糊」滑块完全没反应：透明窗口里 backdrop-filter 不生效，而桌面帧此前没有接进玻璃，两个滑块在渲染链路上是死控件
- 通知弹窗右上角的时间小字改为与正文同色，不再发灰
- 「消息通知设置」的默认值改为与本机当前设置逐项一致，按「恢复默认」不会再改动任何观感
- 弹窗卡片四周留白随投影强度变化，窗口跟着卡片一起长大，投影不会再被切掉
- 新增性能基准 `npm run bench`（约 50 秒）：启动、切页、弹窗、满档玻璃、候选筛选五类指标带基线对比与回归门限，每次大改动收尾跑一次

# Weport v1.0.0

> Windows 产物已本机实测（安装 / 导出 / 弹窗 / 克隆 / 内存）；macOS·Linux 运行时行为见 `docs/v1.0.0-unverified.md`。

- 全新界面：左侧分组导航取代 8 个平级页签，全局连接状态合并到一处
- 外观可自定义：背景图（自带对比度遮罩）、强调色、界面密度；主题收敛为「明暗 × 强调色」12 张卡加自定义
- 「浓郁用色」扩到面板、页头、导航、字段、按钮与进度条；设置页各分块统一成一张卡
- 新增 WeBot 定时任务：任务编辑器、执行频率、错过补偿、串行派发与运行历史
- 新增 WeBot 笔记板：任务结论以卡片留存，支持置顶已读，并通过本地 HTTP API 与 MCP 只读暴露
- WeportAI 与 WeBot 输入框支持 `@` 引用会话；WeBot 完成或失败复用右上角弹窗
- 新增「连接器」，首发 Todoist：粘贴令牌即可，支持项目 / 标签 / 收件箱、自然语言时间与优先级，可让 AI 与定时任务建待办
- 新增 `weport` 命令行：终端里打开完整界面，也可 `weport <命令> 参数=值` 脚本化使用
- 背景视频新增三档画质（原生 / 平衡 / 精简）与自动降采样：4K 壁纸按显示尺寸转码并缓存，实测掉帧归零
- 背景按实际亮度自动判定明暗，用户手动选过之后不再覆盖
- 弹窗玻璃重做：去掉垫在玻璃里的抓屏快照，填充压到 5% 以内并改用 1px 内描边，卡片第一次真正「透」
- 弹窗文字颜色不再随桌面自动变化，改由用户选的填充色决定；新增渐变填充（8 套预设加自定义，仅从左到右）
- 修复通知弹窗背景变黑：原生面板改为显式开启，默认走 Chromium 合成路径，最差只是「没有快照」
- AI 服务商层重做：按模型选择协议、models.dev 元数据、真实价格与上下文窗口；顶栏显示单价与本轮花费，未收录模型写「未定价」
- 重写 AI 提示缓存策略，长任务稳态命中率显著提升；对话标题改为 2-4 词主题概括
- Agent 面板新增「压缩上下文」、输出速度与首 token 耗时读数；记忆文件删除改为二次确认
- 人格克隆语料重做：只取本人真实发言并按「对方说了什么 → 本人怎么回」成对组织；留出 30 轮实测字面相似度 0.069 → 0.194、回复长度 8.4 倍 → 2.6 倍
- 人格克隆新增内置对话入口与对话历史，并跟随对方语言（英文回英文、中文回中文）
- 人格克隆改用「设置 → AI 服务」里配好的服务，不再被锁在一个按地区拒绝的网关上
- 导出：进度移到吸顶页头下方，消除导出期间整页抖动，完成态不再停在「准备中… 189 / 189」
- 性能：首屏 JS 从 1751KB 降到 181KB，实测 FCP 2312 → 712ms；朋友圈发布者侧栏改虚拟滚动，229 行挂载降到 18 行
- 修复「朋友圈」左侧筛选栏整块塌陷、「防撤回」列表不显示头像以及头像被横向拉伸十倍
- 修复「设置 → AI 服务」提供商面板整体错乱、添加提供商弹窗掉到屏幕外，以及 WeportAI 整页没有玻璃感
- 修复「服务分配」为功能面单独指定的服务不生效，以及模型报错被统一成「密钥无效」而掩盖上游原因
- 修复浅色主题下背景图不显示、图片背景的「背景模糊」滑块无效、背景遮罩方向相反
- 修复 macOS 安装包签名（此前完全未签名，系统提示「已损坏」）、误选微信 3.x 遗留目录、只读安装位置下 WCDB 宿主无法启动
- 修复「获取图片密钥」失败时不说卡在哪一步，改按账号分别解析并给出下一步（issue #20、#15）
- `-3999` / `-3998` 不再只显示一个数字，会附带具体原因；README 增加完整错误码表（issue #17）
# Weport v0.9.11

- 修复 macOS 导出图片时不提示获取图片密钥的问题，并接通 macOS/Linux 图片密钥内存扫描
- 消息通知可自动遵循微信「消息免打扰」，新增「仅 @我」过滤模式，会话选择按最近聊天排序
- 通知弹窗时长改为可输入秒数，默认显示 3 秒
- 修复分析缓存未随新消息失效、活跃天数估算不准和星期分布偏移，并让强制刷新覆盖时段统计
- 修复关闭消息通知后新群自动防撤回停止监控的问题
- 修复 macOS 安装包 WCDB 动态库依赖，完善 SIP 失败提示与最低系统要求
- 完善 Linux 微信目录和进程识别，避免重复启动或强制结束微信，并加入 XDG 开机自启
- 补充 macOS/Linux 运行依赖与排障说明；自动回复因缺少安全、稳定的跨平台发送接口暂不集成
- 新增「更新日志」查看器：检查更新自动弹出，更新说明以 Markdown 预览渲染；关于页可随时回看完整历史
- 修复通知弹窗沿用旧头像的问题；更换头像后会自动刷新
- 会话过滤选择器显示会话头像（群聊为方圆角）；朋友圈侧栏新增发布者搜索

# Weport v0.9.10

- 新增 Linux x64 支持，提供 AppImage 与 tar.gz 安装包
- 修复自动获取密钥后数据库未立即连接的问题；手动粘贴密钥现在可确认并验证会话读取
- 修复通知弹窗右侧边缘被裁切；支持右键关闭当前弹窗，并可配置位置、显示时长与动效
- 完善第三方依赖许可证与来源说明，随安装包提供对应许可文本

# Weport v0.9.9

- 修复点击获取密钥闪退，现在会提示错误并记录日志
- 修复中文用户名/OneDrive 路径下导出显示空白，消息库现在能正常发现
- 补上图片导出功能，新增获取图片密钥按钮，未配置时会提醒
- 修复 HTML 导出日历无法跳转，改为二分查找与分帧渲染，大会话不再卡死
- 加固密钥提取与内存扫描的稳定性，修复多处小概率崩溃与卡死
- 优化头像缓存与数据库连接恢复，减少跨账号与宿主重启后的异常

# Weport v0.9.8

- 修复导出时顶部“收集消息·群名”挤压跳动，进度改为固定单行的当前会话；默认窗口最大化，分析入口改为一行四列
- 朋友圈发布者列表启动即按发帖数正确排序；WeportAI 侧边“调试日志”改为“日志”
- AI 添加提供商改为独立弹窗，支持目录挑选、密钥验证与模型拉取
- 拆分 OpenCode Zen / Go 为独立提供商（不同网关地址与模型列表）
- 重做双人年度报告布局，修复月度多 key 兼容、7×24 热力图渐变与图例，头像改为本地加速
- 联系排行榜切换群聊改为本地缓存切片，提速；活跃日历精简图例、紧凑布局
- 加宽全部柱状图（稀疏与横向尤其加粗），“我的每日消息”回归平滑折线
- 高频词云改为统计用户本人发送的全部文本（私聊+群聊、无排除过滤）
- 全量悬停补齐 0.15s 平滑过渡，统一动效与入场动画

# Weport v0.9.7

- 修复删除最后一个 WeportAI 提供商后旧配置被自动恢复的问题
- 将导出范围改为紧凑弹出菜单，保留搜索、筛选与虚拟列表
- 修复双人年度报告微信号居中显示，并统一文档中的 Weport 项目名称

# Weport v0.9.6

- 导出数据支持按会话选择，并让文本导出真正使用受控并发
- 重做数据分析布局、词云、排行榜与年度/双人报告入口
- 新增新群自动防撤回、WeportAI 多提供商配置与模型发现
- 修复黑白主题重启后的状态同步，并更新 README 截图

# Weport v0.9.5

## 分析页新增可视化（词云 / 画像雷达 / 活跃日历 / 群热力图）

- 全局分析：新增交流画像雷达（文字/图片/语音/视频/表情/深夜活跃六维）、活跃日历（近 12 个月每日消息量热力）、高频词云（CJK 二元分词 + 停用词过滤，最多扫描 15 万条文本消息，10 分钟缓存）
- 群聊分析：新增「画像」页签（成员画像雷达 + 24×7 活跃热力图）；成员详情新增高频词云（Top 40）
- 全部新图表跟随浅蓝/黑白主题；echarts-wordcloud 在 ECharts 6 下验证可用

## 本地 MCP 服务（只读，AI 宿主接入）

- 基于 @modelcontextprotocol/sdk 的 Streamable HTTP 服务：`http://127.0.0.1:5032/mcp`，Bearer token 认证（`mcpToken` 自动生成并 safeStorage 加密，可回退 `httpApiToken`）
- 13 个只读工具：会话列表 / 会话消息 / 消息搜索 / 联系人 / 群聊列表 / 群成员 / 群统计（时段+媒体+热力图）/ 朋友圈时间线 / 朋友圈统计 / 全局统计 / 联系排行榜 / 每日活跃 / 高频词 —— 全部落在既有只读服务上，无写入/发送能力
- Claude Desktop 等仅支持 stdio 的宿主：通过 `resources/mcp/mcp-stdio-bridge.cjs` 桥接（esbuild 单文件自包含，任意 Node ≥ 18 可运行，无需 NODE_PATH）
- 配置键：`mcpEnabled`（默认开）、`mcpPort`（5032）、`mcpHost`（127.0.0.1）、`mcpToken`（自动生成）

# Weport v0.9.4

- 修复自动更新下载完成后不安装、不重启的问题；托盘静默启动首次打开窗口时会补查更新。
- 优化朋友圈启动与加载：缓存优先、缩略图优先，完整图片和视频只在预览或下载时获取。
- 修复 Windows 版朋友圈 WASM 解密资源缺失、解密失败重试和媒体重复下载问题。
- 优化朋友圈新动态检测、作者头像、评论表情和群成员头像缓存，减少重复数据库与网络请求。
- 优化启动阶段的数据库连接、会话读取和后台预热，避免多个任务争抢 WCDB 宿主。
- 群聊分析复用聚合结果，修复缓存命中误报失败，并补齐成员头像本地化。
- 朋友圈新增持久化页面/作者缓存、日期跳转、评论搜索、虚拟滚动和手动刷新。
- WeportAI 兼容更多 OpenAI 接口：规范化请求地址，隔离 DeepSeek 专用参数，修复不完整工具历史导致的 400，并保留更完整的错误信息。
- 新增双人年度报告、数据备份、本地 HTTP API 和 Windows Hello 认证能力。

# Weport v0.9.3

## 内存优化（实测常驻 ≈524 MB，较 v0.9.2 的 ≈844 MB 下降 38%）

### WCDB 宿主进程改为纯 Node 模式

- 宿主仍由硬链接 `WeFlow.exe` 启动（-1006 文件名检查不变），但以 `ELECTRON_RUN_AS_NODE=1` 运行 —— 同一二进制按纯 Node.js 执行，不再初始化 Chromium 浏览器进程
- 实测：宿主工作集 104.1 MB + 网络服务子进程 50.6 MB → **44.9 MB 单进程**（省 ≈110 MB）
- koffi（N-API）无需重编译；打包版宿主脚本与 koffi 复制到 `resources/host/`（纯 Node 读不了 app.asar），经 `NODE_PATH` 解析

### 隐藏窗口内存回收（托盘模式）

- 主窗口隐藏到托盘超过 5 分钟 → 卸载渲染层（`about:blank`），省掉渲染进程堆与 GPU 缓冲；托盘点击 / 二次启动即恢复（重载后约 0.5s 显示）
- 导出进行中不回收（进度事件目标需存活）；QA/截图模式不回收；状态全部在主进程/配置侧，恢复无功能丢失

### `--background` 关闭硬件加速

- 开机自启托盘常驻时调用 `app.disableHardwareAcceleration()`：GPU 进程消失（实测省 ≈130 MB 工作集 / ≈312 MB 私有提交），窗口走软件光栅；Windows 通知弹窗的原生玻璃面板不受影响（D3D11 在原生侧）

### Chromium 基础调优

- `js-flags --max-old-space-size=384 --max-semi-space-size=4`：V8 堆封顶，提前触发 GC 降低稳态内存
- `disk-cache-size 16MB`：HTTP 缓存上限（头像已走本地磁盘缓存）
- 两个窗口 `spellcheck: false`：关闭拼写服务与词典内存

### 通知弹窗独立瘦身入口

- 弹窗改加载 `dist/popup.html`（独立 vite 入口，仅 NotificationWindow 依赖），不再解析含 ECharts/html2canvas 的整包 —— 弹窗渲染进程堆显著下降，首条通知出现更快
- 弹窗字体与主应用保持一致（`popupBase.css`，修复此前字体回退到系统默认的问题）

## 修复

- 截图 QA 的 AI 面板诊断探针曾内嵌 TypeScript 类型注解（`as`/`: Element`），探针本身报 SyntaxError 掩盖真实原因 —— 已改为纯 JS

## 验证

- 类型检查 + 完整截图冒烟（12 屏全通过，弹窗 stddev ≥ 60）
- 宿主纯 Node 冒烟：`wcdb_init [SecurityStatus:0]`（-1006 文件名检查通过）
- 实测内存（托盘 + 开窗 + 推送常开）：主进程 137 + 渲染 105 + 弹窗 77 + 宿主 77 + 工具进程 128 ≈ **524 MB**（v0.9.2 同场景 ≈844 MB）

# Weport v0.9.2

## 稳定性与安全加固（25 项问题审查 + 修复）

### 安全
- **AI 聊天 Markdown XSS 修复**：链接/图片 URL 协议白名单（拦截 `javascript:`/`data:`/`file:`）、失败回退纯文本转义、渲染层 CSP（生产严格策略）、主/通知窗口导航守卫（外链一律系统浏览器打开）
- **WeClone CSV 公式注入防护**：以 `= + - @` 制表符/回车开头的内容加撇号前缀，恶意会话内容不再能被 Excel 当公式执行
- **AI API 地址安全校验**：仅允许 https（localhost 例外），密钥不再可能明文传输；隐私文案修正（密钥会随请求发给所配置的 API）

### 导出可靠性
- **中途失败不再产出"完整"残档**：批次读取失败显式上报错误（此前静默截断，部分行被当作完整导出写入）；8 个格式器统一检查
- **原子写入**：TXT/Markdown/SQL/WeClone/HTML 先写同目录临时文件再重命名覆盖；写中途失败保留旧文件、流错误立即捕获（此前崩溃/挂死或截断旧档）
- **同名会话不再互相覆盖**：同一批导出内同名会话（如多个「一家人」）自动加序号去重
- **账号切换错位修复**：选择账号时把该账号密钥写回全局配置（此前导出/连接可能用到上一个账号的密钥）；仅在实际不一致时写回，避免启动连接抖动
- **CSV/导出记录按账号隔离**（wxid 前缀）；Windows 保留设备名（CON/PRN/NUL 等）自动改名；更新版本比较支持预发布后缀
- **导出可取消**：进度事件携带 taskId，导出页新增「取消导出」按钮；取消后自动清理本次已写出的部分文件；未知任务 ID 不再假成功

### WCDB 宿主进程
- **请求串行化**：原生 core 不可重入，所有宿主消息排队执行（此前 testConnection/close 可能与其他在途请求交错，打到空句柄）
- **超时自动重建**：180s 卡死不再需要重启应用；宿主退出（含退出码 0）立即失败所有在途请求
- 宿主硬链接新鲜度检查增加修改时间维度；关闭消息提醒后释放 WCDB 连接与监控管道

### WeportAI
- 切换会话自动中止旧任务（此前 running 卡死、停止按钮打错目标）；删除运行中的会话会先中止
- 流式响应：无数据 90s 看门狗（服务端挂起不再无限等待）、流末尾无换行的尾块不再丢弃
- 会话索引/消息文件原子写入（崩溃不再剩半个 JSON）；快速切换时丢弃过期响应

### 其他
- 通知弹窗：底部定位随高度变化重新贴底；玻璃背景不再把弹窗自己拍进去（截图时临时启用内容保护）
- `--background` 静默启动后手动启动应用会唤出主窗口（此前无反应）
- 事件订阅退订只移除本回调（此前 removeAllListeners 会误杀同频道其他订阅者）

## 朋友圈媒体解密修复与提速

- **灯箱「图片无法查看」修复**：点击图片此前会用加密的 CDN 密文 URL 渲染（必然破图）。现在灯箱直接使用网格已解密的本地全图（weport-media:// 毫秒级显示），前后翻页同样命中本地文件；视频标记正确
- **「加载失败」修复**：32 位字级 XOR 曾用 `writeUInt32BE` 导致符号位溢出抛错（约半数字节块崩溃）——改为 Uint32Array 视图 XOR，同时提速约 4.7 倍（实测 2ms/4MB）
- **解密提速**：keystream 按密钥缓存（LRU 64MB，同密钥媒体免重复生成）；CDN 下载并发限 4（此前几十张并发争抢带宽触发超时）；超时 15s→30s；失败资源 5 分钟快速失败记忆（死链不再反复重试）
- 真实数据验证：3 张真实 CDN 图片解密全部成功，缓存命中 0–2ms

## 分析界面（微信数据分析）

- 入口两个卡片（全局分析 / 群聊分析）**始终并排**：修复 auto-fit 网格在 flex 居中容器内塌缩成单列的问题，全屏与非全屏均保持左右并排
- 群聊分析：顶栏收窄靠右，群列表占满左侧；新增排序切换（消息数 / 人数，默认消息数）；群列表显示消息条数
- 媒体构成：饼图改用可区分的专属色板（文本/图片/语音/视频/表情包/链接文件各一色，黑白主题灰阶）；列表每类配专属图标
- 24 小时活跃度柱形加宽、柱间距大幅缩小
- 成员列表头像提速：去掉每次进群都强制全量重算（缓存命中即时返回）

## 交互动效补全

- 开关/勾选框：打勾瞬间弹跳 + 文字轻弹（此前完全无反馈）
- 通用按压反馈：主要按钮/图标按钮/分段控件按下缩放；页签激活弹跳动画

## 表情显示修复（上版内容保留）

- 补齐全部微信表情资源：此前只打包了 `face` 类别（75 个），动物 / 祝福 / 手势 / 其他等类别缺失导致部分表情（[动物] 等）显示为空白/破图。现在与 WeFlow 一致，完整打包 wechat-emojis 全部 109 个表情（5 个类别），朋友圈正文、评论与通知弹窗中的表情均可正常显示。

## 朋友圈布局与体验

- 顶部横条（总动态 / 好友 / 我的动态 + 防删除 / 导出）合并进左侧筛选栏顶部，整体高度大幅缩减，信息更紧凑
- 评论区的自定义表情包显示修复（本地协议链路统一）

## 朋友圈媒体加载提速（对齐 WeFlow 并优化）

- 网格缩略图：大图在**主进程**用 nativeImage 一次性缩到 720px（JPEG），IPC 载荷与渲染内存降低 20~50 倍，网格不再长时间停留在「解密中」；全图始终写入磁盘缓存
- 灯箱预览改为直接读取磁盘缓存全图（weport-media:// 本地协议，零网络零 IPC）；下载也走全图文件而非缩略图
- 视口懒加载、内存缓存与磁盘缓存命中路径保持不变（不增加 CPU / RAM 常驻开销）

## 分析（微信数据分析）

- 「确定性数据分析」更名为「微信数据分析」，入口标题上方新增浮动动画图标
- 消息类型构成：六类消息全部配图标 + 专属主题色；统计卡片 / 排行榜 / 成员数字全部着色并带悬停动效
- 卡片 hover 过渡补全：标题、描述、箭头随图标一起平滑动画

## 色彩主题（双主题）

- 新增「设置」页签（顶栏最右侧），独立于「消息通知」
- 设置页含：开机自启 / 启动时隐藏到托盘 / 关闭最小化到托盘 + 色彩主题 + 关于更新
- 色彩主题两种模式：**缤纷**（默认，现代六色 accent 体系，克制不刺眼）与**黑白**（经典灰阶），应用于文字、图标、图表、数字的整个 UI；选择即时生效并持久化保存

## 其他修复

- 开机自启：清理参考项目 WeFlow 遗留的登录项（此前会随系统开机拉起 WeFlow）；修复 Run 键比较逻辑（此前每次启动都会重写）；开关以系统实际状态为准，失败不再误显示「已开启」
- 数字滚动动画（CountUp）跳动期间恒为整数
- 新增 `npm test`：类型检查 + 截图冒烟测试一键执行

# Weport v0.9.1

## 头像加载深度修复（消息弹窗 / 朋友圈 / 群聊 / 排行榜）

此前头像总是「转圈 / 占位一段时间」才显示，根因有三层，本次全部修复：

1. **本地头像优先** — 微信 `head_image.db` 里本来就有每个人的头像数据（实测 87%–100% 覆盖率），此前只在「没有 CDN URL」时才读取。现在优先读取本地头像，离线、不过期、即时显示。
2. **头像磁盘缓存 + 本地协议** — 新增 `avatarCacheService`：所有头像（head_image 或 CDN）统一落盘到 `cache/avatars/`，通过 `weport-media://` 本地协议即时渲染。CDN URL 过期 / 403 不再影响显示；渲染进程零网络请求。启动预热（前 600 会话）自动完成全部头像落盘，之后弹窗、朋友圈、群成员、排行榜均为毫秒级加载。
3. **关键修复：本地协议 URL 格式** — 旧格式把盘符放进 host（`weport-media://C:/…`），Chromium 会把 `C:` 规范化为 host `c`（冒号被当作端口分隔符），路径解析丢失盘符冒号 → 永远 404。新格式 `weport-media://local/<百分号编码的完整路径>` 彻底解决，同时修复了朋友圈视频预览的同类隐患。
4. **渲染层提速** — 头像加载队列并发 3 → 8、批间延迟 10ms → 2ms；本地协议 URL 不再排队直接加载。
5. **缓存命中校验** — 缓存里的本地 URL 若文件已不存在（如缓存目录变更）会自动重新解析，不会显示破图。

实测（真实数据库）：60 个会话的探针样本 100% 返回本地协议 URL、100% 文件可解析、0 个 CDN 回退；283 个头像文件落盘。

# Weport v0.9.0

## 新功能

### 朋友圈（v0.9 新增模块）

- 全量浏览本地微信 sns.db 的历史朋友圈：分页时间线、按发布者 / 关键词 / 日期快速筛选，点击发布者可查看其全部动态
- 媒体解密预览：图片 / 实况照片 / 视频经本地解密后直接在应用内预览（视口懒加载 + 内存/磁盘缓存，滚动长列表时 CPU 与内存占用显著降低）
- 导出：JSON / HTML / ARKME JSON / Markdown 四种格式，支持时间范围预设（全部 / 30 天 / 一年 / 自定义）与媒体文件选择，后台进度、可取消
- 防删除：一键安装朋友圈删除拦截触发器；也可单独删除某条记录
- 旧版朋友圈媒体缓存自动检测与迁移

### 分析（v0.9 新增模块）

- 统一入口：全局分析 / 群聊分析 两个大卡片选择
- 全局分析：消息类型构成、24 小时 / 星期 / 月度分布、我的每日消息、联系排行榜 Top 20、统计排除名单（公众号等一键排除并即时重算）
- 年度报告：年度核心好友、每月聊得最多的人、7×24 活跃热力图、深夜王者、互相奔赴、社交主动性、响应速度、高频短语、朋友圈年度统计；可导出报告图片（按分区 PNG）
- 群聊分析：群成员列表（群主 / 好友标识、消息占比条）、消息排行 Top 20、24 小时活跃时段、媒体构成；点击成员查看画像（统计卡片、活跃时段、高频短语、常用表情、分页消息记录）并可导出 CSV

## 性能与体验

- 朋友圈媒体视口懒加载：进入可视区域（提前 600px）才解密，避免长列表一次性拉取全部媒体
- 列表与弹层组件 React.memo 化 + 稳定回调，减少无效重渲染
- 统计数字滚动动画（CountUp，尊重系统"减少动效"设置，数据后到不重播）
- 页签切换动画平滑化；动态列表入场错峰动画；媒体骨架微光占位
- 所有弹层支持 Esc 关闭；空状态统一为图标化提示
- 顶栏页签可横向滚动（窗口较窄时不挤压）

## 技术

- 服务层（朋友圈 / 全局分析 / 群聊分析 / 年度报告）为 WeFlow 近原样移植，WCDB 引擎层零改动（原生 FFI 此前已就绪）
- 新增 weport-media:// 本地媒体协议（特权注册，webSecurity 保持开启）
- 年度报告生成在独立 worker 线程中运行，进度跨窗口广播
- QA：新增 WEPORT_V09_DUMP=1 端到端转储模式（脱敏演示数据驱动全部 v0.9 页面，断言 DOM 与 console 错误，失败非零退出）

---
