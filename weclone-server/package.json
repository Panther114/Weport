{
  "name": "weclone-server",
  "version": "0.9.10",
  "private": true,
  "description": "WeClone personality-clone chat server — Fastify + JSON-file meta + FS blobs (JSONL/mds) + hand-rolled BM25 + OpenAI-compat LLM proxy (Railway-deployable, <100MB RAM, zero native deps)",
  "main": "dist/server.js",
  "license": "CC-BY-NC-SA-4.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "ts-node src/server.ts",
    "seed": "node scripts/seed-demo.mjs"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.0",
    "@fastify/rate-limit": "^9.1.0",
    "@fastify/static": "^7.0.4",
    "fastify": "^4.28.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "@types/uuid": "^9.0.8",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
