# Nixpacks fallback build (preferred: Dockerfile via railway.json)
[variables]
NODE_VERSION = "18"
NODE_OPTIONS = "--max-old-space-size=256"

[phases.install]
cmds = ["npm install --no-audit --no-fund --loglevel=warn"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node dist/server.js"
